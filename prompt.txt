"Claude, vamos aplicar duas melhorias focadas puramente na Experiência do Usuário (UX) e organização da interface:

1. Textarea Auto-ajustável (Fim da barra de rolagem):
Na tela de criação de habilidades (no campo de Preview/Texto Final editável), o <textarea> está exibindo uma barra de rolagem lateral (scrollbar) e limitando a visão geral do texto.

Altere o comportamento desse <textarea> para que ele ajuste sua altura (height) automaticamente com base no conteúdo inserido.

Você pode fazer isso utilizando um useRef e ajustando o style.height = scrollHeight + 'px' no evento onChange (ou sempre que o texto for atualizado), ou utilizando uma biblioteca como react-textarea-autosize se já estiver no projeto.

Remova o overflow-y que força a barra de rolagem e certifique-se de que a caixa cresça suavemente para baixo conforme o texto aumenta.

2. Reordenação do Grid de Estatísticas (Acerto e CD lado a lado):
Nas 'Estatísticas de Combate' (tanto na ficha pronta quanto no Combat Tracker), a ordem visual atual está separando atributos que costumam ser consultados juntos.

Encontre o componente que renderiza o grid de Estatísticas de Combate.

Mude o card de 'Acerto' de lugar com o card de 'Iniciativa'.

A nova ordem no JSX deve ser: Defesa, Atenção, Acerto, CD, Iniciativa, RD Geral, Ignorar RD, Vida Temp/Atq.

O objetivo é garantir que o Acerto fique posicionado exatamente ao lado da CD na mesma linha visual.

Aplique essa reordenação do JSX e a função de auto-resize no textarea!"